import { Request, Response } from "express";
import { RequestWithBody, User } from "../models";
import { handleError } from "../util/helper";

const createMessage = (req: RequestWithBody, res: Response) => {};

export default { createMessage };

import LoginCard from "../components/LoginCard";

const Home = (): JSX.Element => {
  return (
    <div>
      <h1>This is the home page</h1>
      <LoginCard />
    </div>
  );
};

export default Home;
